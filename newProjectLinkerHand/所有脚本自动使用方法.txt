# 给执行权限
chmod +x monitor.sh

# 运行脚本
sudo bash monitor.sh

# 注意：文件夹中的count.csv是例子

//--------------------------------------------------------------------//

# v1.2更新
# 进度条功能

# 注意：文件夹中的两个bash脚本是用来分别切换路径执行插时长和上传的
# autoTime是插时长
# autoTransform是上传

# 运行脚本
# chmod +x autoTime.sh
# sudo bash autoTime.sh  
# sudo bash autoTransform.sh

# 注意：v1.2目前存在bug，多任务时只能显示第一回遍历到的任务名，bug将会在1.3中修复

//--------------------------------------------------------------------//


